(*) Required Libraries:-
    sqlite3

(*) Software requirements:-
    (-)Spyder
    (-)DB Browser

(*) NOTE: patient_ids(p_id) should be registered/created in 
	  incremental order(0,1,2,..) to avoid error in indexing.

(*) NOTE: All files need to be placed in one folder. Just open
"H_M_S.py" file to execute this project. 

This project is created by Sarpreet Singh.